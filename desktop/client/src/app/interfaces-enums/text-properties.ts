export enum Font {
    Arial = 0,
    TimesNewRoman = 1,
    ComicSansMs = 2,
    CourierNew = 3,
    Impact = 4,
}

export enum Emphasis {
    Italic = 0,
    Bold = 1,
    ItalicBold = 2,
    Normal = 3,
}

export enum TextAlign {
    Left = 0,
    Center = 1,
    Right = 2,
}

export enum CanvasType {
    previewCtx = 0,
    baseCtx = 1,
}
